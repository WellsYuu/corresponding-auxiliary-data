<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.express">	<select id="manage.express.selectPageList" parameterType="com.jiagouedu.services.manage.express.bean.Express" resultType="com.jiagouedu.services.manage.express.bean.Express">	 select * 	 from t_express t where 1=1  order by order1   limit #{offset},#{pageSize} 	</select> 	<select id="manage.express.selectPageCount" parameterType="com.jiagouedu.services.manage.express.bean.Express" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_express t where 1=1 	 ) a 	</select> 	<insert id="manage.express.insert" parameterType="com.jiagouedu.services.manage.express.bean.Express" useGeneratedKeys="true" keyProperty="id">		insert into t_express		(		id,fee		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="order1!=0">		,order1		</if>		)		values		(		id,#{fee}		<if test="code !=null and code !=''">		,#{code}		</if>		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="order1 !=0">		,#{order1}		</if>	)</insert>	<update id="manage.express.update" parameterType="com.jiagouedu.services.manage.express.bean.Express">		update t_express		  set id=#{id},fee=#{fee}		<if test="code !=null and code !=''">		,code=#{code}		</if>		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="order1 !=0">		,order1=#{order1}		</if>		 where 1=1 and id=#{id}	</update>	<select id="manage.express.selectOne" parameterType="com.jiagouedu.services.manage.express.bean.Express" resultType="com.jiagouedu.services.manage.express.bean.Express">	 select * from t_express t where 1=1 and id=#{id}	</select>	<delete id="manage.express.deleteById" parameterType="com.jiagouedu.services.manage.express.bean.Express">		delete from t_express where 1=1 and id=#{id}	</delete></mapper>